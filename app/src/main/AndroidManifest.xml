<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.clara.patalocalizada">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permissão para usar a câmera -->
    <uses-permission android:name="android.permission.CAMERA" /> <!-- Declara que a câmera é opcional para dispositivos que não a têm -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@drawable/logo1"
        android:supportsRtl="true"
        android:theme="@style/Theme.PataLocalizada"
        tools:targetApi="31">
        <activity
            android:name=".LostAnimal"
            android:exported="false"
            tools:ignore="Instantiatable" />
        <activity
            android:name=".LostAnimalsListActivity"
            android:exported="false" />
        <activity
            android:name=".ReportLostAnimalActivity"
            android:exported="false" /> <!-- Atividades da aplicação -->
        <activity
            android:name=".QrResultActivity"
            android:exported="false" />
        <activity
            android:name=".ScanActivity"
            android:exported="false" />
        <activity
            android:name=".MoreDetailsActivity"
            android:exported="false" />
        <activity
            android:name=".AdoptionActivity"
            android:exported="false" />
        <activity
            android:name=".HomeActivity"
            android:exported="false" />
        <activity
            android:name=".SignupActivity"
            android:exported="false" />
        <activity
            android:name=".LoginActivity"
            android:exported="false" />
        <activity
            android:name=".AnimalDetailActivity"
            android:exported="false" /> <!-- Atividade principal (launcher) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>